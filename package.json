{
  "name": "n8n-radware-agentic-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Reference deployment of Radware Agentic AI Protection for n8n: in-path enforcement plus an out-of-path guard node for per-tool interception",
  "license": "MIT",
  "packageManager": "pnpm@11.24.0",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "mock": "node scripts/mock-digester.mjs",
    "validate:live": "node scripts/validate-live.mjs",
    "hash-password": "node scripts/hash-password.mjs"
  },
  "devDependencies": {
    "bcryptjs": "^3.0.3"
  }
}
