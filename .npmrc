# Community node peer deps (n8n-workflow, @n8n/ai-node-sdk) are provided by the
# n8n runtime at load time, not bundled. Do not let pnpm hoist or install them.
auto-install-peers=false
strict-peer-dependencies=false
